# CSV Cell Viewer — 매뉴얼

*언어: [English](MANUAL.en.md) · **한국어***

CSV Cell Viewer는 단일 `index.html` 파일입니다: 설치·빌드 단계·의존성·네트워크 접근이 전혀 없으며,
브라우저에서 열면 오프라인으로 동작합니다. 이 매뉴얼은 모든 기능을 자세히 설명합니다. 간단한 소개는
[README](readme.md)를 참고하세요.

---

## 1. 왜 만들었나

스프레드시트 앱은 수천 개의 셀로 이루어진 격자를 한꺼번에 렌더링합니다. 셀에 길거나 여러 줄이거나
한글/CJK 텍스트가 들어 있으면, 이 레이아웃 계산 때문에 스크롤과 선택이 느려집니다. CSV Cell Viewer는
**한 번에 셀 하나**만 큰 영역에 그리므로, 파일이 아무리 크거나 빽빽해도 표시 비용이 일정합니다. 여기에
원본 데이터를 전혀 건드리지 않고 행에 **주석/라벨**(코멘트, 분류, 검토 표시)을 빠르게 다는 키보드 중심
작업 흐름을 더했습니다.

## 2. 시작하기

1. `index.html`을 엽니다(더블클릭, 또는 폴더를 서빙해 접속).
2. **Open CSV…**를 클릭하거나 `.csv` 파일을 창에 끌어다 놓습니다.
3. 첫 데이터 셀이 큰 **원본 영역**에 표시됩니다. 방향키로 이동하세요.

**브라우저 참고**

- 모든 최신 브라우저(Chrome, Edge, Firefox, Safari)에서 동작합니다.
- **원본 파일에 바로 저장**(원본 파일에 다시 쓰기)은 Chromium 계열 브라우저(Chrome / Edge)이면서
  앱 내 **Open CSV…** 선택창으로 파일을 연 경우에만 가능합니다. 그 외 환경에서는 저장 시 다운로드로
  대체됩니다(§9 참고).
- 모든 처리는 로컬에서 이루어지며, 파일은 브라우저에서 읽힐 뿐 업로드되지 않습니다.

## 3. 화면 구성

- **툴바(상단).** 파일 열기 + 이름, 인코딩, 구분자, 보기 토글(머리글, 공백 정리, 문장 줄바꿈), 글자
  크기, 복사 / 다운로드 / 저장 + BOM, 그리고 테마 토글.
- **주소 표시줄.** 현재 원본 셀: 스프레드시트식 좌표(예: `B5`), **모드 배지**(`NAV` 또는 `EDIT`),
  맥락 정보(열 이름, 데이터 행 위치, 열 위치).
- **원본 영역(읽기 전용).** 현재 셀의 전체 내용을 저장된 그대로 표시합니다(§6의 표시 전용 토글만
  적용). 이 영역은 절대 편집되지 않습니다.
- **분할선.** 원본 영역과 주석 영역 사이의 손잡이를 끌어 크기를 조절합니다.
- **주석 영역(편집 가능).** **빠른 라벨 막대**(`1`–`9`)와, 현재 행의 *주석 열*에 연결된 **편집 가능한
  텍스트 상자**.
- **이동 줄.** 화면상의 ◀ ▲ ▼ ▶ 버튼과 레코드 / 열 개수 표시.
- **사이드 패널(우측).** 주석 열 선택, 빠른 라벨 프리셋 편집, 자동 다음 행 이동 토글. `⟩`로 접고,
  오른쪽 가장자리의 `⟨` 탭으로 다시 엽니다. 왼쪽 가장자리를 끌어 너비를 조절합니다.
- **상태 표시줄(하단).** 최근 동작 메시지와 단축키 힌트.

## 4. 파일 열기

**인코딩.** `Auto`(기본값)는 먼저 BOM(바이트 순서 표식)을 확인한 뒤, 엄격한 UTF-8을 시도하고 실패하면
**EUC-KR / CP949**로 대체합니다 — 윈도우 엑셀에서 내보낸 한글 CSV가 주로 쓰는 인코딩입니다. `UTF-8`이나
`EUC-KR / CP949`로 직접 지정할 수도 있습니다. 글자가 깨져 보이면(모지바케) 수동으로 바꾸세요.

**구분자.** `Auto`(기본값)는 첫 줄에서 쉼표, 탭, 세미콜론, 파이프를 감지합니다. 직접 지정할 수도 있습니다.

**머리글 행.** 켜면(기본값) 첫 행을 열 이름으로 취급하여 주소 표시줄과 주석 열 선택기에 사용합니다.
머리글이 없는 파일이면 끄세요. 이 토글은 데이터를 다시 읽거나 변경하지 않습니다.

> **인코딩**이나 **구분자**를 바꾸면 파일을 다시 읽습니다. 저장하지 않은 편집이 있으면 먼저 확인을
> 요청합니다.

## 5. 원문을 그대로 읽기

파서는 RFC 4180을 따릅니다.

- 따옴표로 감싼 필드는 구분자, 줄바꿈, 이스케이프된 따옴표(`""` → 실제 `"` 한 개)를 포함할 수 있습니다.
- 여러 줄 셀은 보존되며, 셀 내부의 줄바꿈은 표시를 위해 `\n`으로 정규화됩니다.
- 파일 끝의 줄바꿈이 가짜 빈 행을 만들지 **않습니다**.
- 파서는 공백을 **제거하지 않습니다** — 보이는 것이 곧 저장된 내용입니다. (선택 사항인 *공백 정리*는
  표시에만 영향을 줍니다. §6 참고.)
- 들쭉날쭉한 행도 처리합니다: 없는 셀은 빈 값으로 읽고, 격자 너비는 가장 넓은 행을 기준으로 합니다.

## 6. 표시 옵션 (표시 전용 — 데이터는 절대 바뀌지 않음)

**공백 정리(Trim view)** *(기본 켜짐).* 셀 *내부*의 빈 줄(공백만 있는 줄)을 제거하고 앞뒤 공백을
정리하여, 빈 줄로 채워진 셀이 실제 내용으로 압축됩니다. 내용이 있는 줄과 그 들여쓰기는 보존됩니다.
끄면 셀을 한 글자도 빼지 않고 그대로 봅니다.

**문장 줄바꿈(Sentence breaks)** *(기본 꺼짐).* 빽빽한 문단을 훑어보기 쉽도록 문장 끝 문장부호
(`.`, `?`, `!`, `…`) 뒤에 줄바꿈을 넣습니다. 문장부호 뒤에 공백이 있고 그다음이 문장 시작처럼 보일
때(대문자, 숫자, 여는 따옴표, 또는 CJK 문자)에만 줄을 바꾸며, 다음은 의도적으로 **건너뜁니다**.

- 소수 — `3.14`(뒤에 공백이 없으므로 영향 없음);
- 흔한 약어 — `Dr.`, `e.g.`, `etc.` 등;
- 점이 있는 약칭 — `U.S.`, `Ph.D`;
- 한 글자 이니셜 — `A.`;
- 단독 숫자 / 서수 / 연도 — `1.`, `1990.`.

문장 중간을 끊느니 차라리 줄바꿈을 놓치도록 보수적으로 설계되어 있습니다(예: `example.com. Next`는
그대로 둡니다).

**글자 크기.** `−` / `+`, 8 ~ 48px; 두 영역 모두에 적용됩니다.

이 세 가지는 표시되는 모습에만 영향을 주며, 저장된 데이터·사본·내보내기에는 영향을 주지 않습니다.

## 7. 이동하기

텍스트 입력 중이 아닐 때(배지가 `NAV`) 이동이 동작합니다.

| 키 | 동작 |
|---|---|
| `←` `↑` `↓` `→` | 셀 한 칸 이동 |
| `Home` / `End` | 행의 첫 / 마지막 열 |
| `Ctrl/⌘ + Home` / `End` | 첫 / 마지막 행 |
| `PageUp` / `PageDown` | 10행 이동 |

화면상의 ◀ ▲ ▼ ▶ 버튼도 동일하게 동작합니다. **머리글 행**을 켜 두면 머리글로 이동해 열 이름을 볼 수는
있지만, 머리글에는 주석을 달 수 없습니다.

## 8. 주석 달기 / 라벨링

핵심 작업 흐름: 원본 셀을 읽고, **원본은 건드리지 않은 채** 그 행에 라벨이나 코멘트를 기록합니다.

**1) 주석 열 선택.** 사이드 패널의 **Annotation column**에서 열을 고르거나, **➕ Add new column…**으로
새 열을 추가합니다(권장 — 원본 데이터를 전혀 건드리지 않습니다). 빠른 라벨과 코멘트는, 지금 어떤 열을
보고 있든, 현재 행의 이 열에 기록됩니다.

**2) 빠른 라벨.** **Quick-insert labels**에서 프리셋 목록을 편집합니다(최대 9개). `NAV` 모드에서
`1`–`9`를 누르거나 빠른 라벨 막대의 버튼을 클릭하면 해당 라벨이 현재 행의 주석 열에 기록됩니다.
**Advance to next row**가 켜져 있으면(기본값) 커서가 다음 행으로 이동해 빠르게 라벨을 달 수 있습니다.

**3) 자유 텍스트 코멘트.** `Enter`(또는 `F2`, 또는 주석 상자 클릭)를 눌러 현재 행에 코멘트를 입력합니다.
배지가 `EDIT`로 바뀝니다. 셀이 여러 줄일 수 있으므로 **`Enter`는 줄바꿈을 입력합니다.** 확정은 다음으로:

- `Ctrl/⌘ + Enter` — 확정하고 다음 행으로 이동;
- `Esc` — 확정하고 이동 모드로 복귀;
- `Tab` / `Shift + Tab` — 확정하고 다음 / 이전 셀로 이동.

코멘트는 입력하는 즉시 저장됩니다.

**불변성.** 선택한 주석 열 외에는 어떤 열에도 쓰지 않습니다. 지금 읽고 있는 긴 여러 줄 셀을 포함해 다른
모든 열은 변경되지 않음이 보장됩니다. (대상으로 *기존* 열을 고르면 당연히 그 열에 쓰게 됩니다. 모든
원본을 그대로 두려면 **Add new column**을 사용하세요.)

## 9. 저장 및 내보내기

파일 이름 옆의 점(`●`)은 저장하지 않은 변경이 있다는 뜻이며, 편집 상태로 창을 닫으려 하면 경고합니다.

**저장(Save)** (`Ctrl/⌘ + S` 또는 버튼):

- Chromium 계열 브라우저에서 **Open CSV…**로 연 파일이라면, **같은 파일**에 다시 씁니다(권한을 한 번
  요청할 수 있습니다).
- 그 외에는 **다운로드**로 대체됩니다.

**다운로드(Download)**는 항상 브라우저 다운로드 방식으로 사본을 저장합니다.

**출력 형식.** 파일은 **UTF-8**, `CRLF` 줄바꿈, RFC 4180 따옴표 규칙으로 저장됩니다(구분자·따옴표·줄바꿈을
포함한 필드는 따옴표로 감싸고, `"`는 `""`로 변환). 구분자는 파일을 읽을 때 사용한 것과 동일합니다.

- **BOM** *(기본 켜짐)*은 UTF-8 BOM을 앞에 붙여 윈도우 엑셀이 UTF-8을 올바르게 인식하도록 합니다. 다른
  도구가 BOM을 싫어하면 끄세요.
- 저장은 항상 UTF-8로 출력되므로, EUC-KR / CP949로 읽은 파일은 저장 시 **UTF-8로 변환**됩니다.

**셀 복사(Copy cell)** (`Ctrl/⌘ + C` 또는 버튼)는 현재 원본 셀의 원문을 클립보드에 복사합니다.

## 10. 레이아웃과 외관

- **테마.** `☾` / `☀` 버튼으로 라이트 / 다크를 전환합니다. 기본적으로 운영체제 설정을 따르며, 한 번
  직접 선택하면 그 선택을 기억합니다.
- **영역 크기 조절.** 원본 영역과 주석 영역 사이의 가로 손잡이를 끕니다.
- **패널 크기 조절.** 패널의 왼쪽 가장자리를 끕니다.
- **패널 접기.** 설정이 끝나면 `⟩`(패널 머리부분)로 숨기고, 오른쪽 가장자리의 `⟨` 탭으로 다시 엽니다.
  빠른 라벨 막대는 본문 영역에 있으므로 접은 상태에서도 라벨링이 가능합니다.

## 11. 설정과 개인정보

다음 설정은 브라우저의 `localStorage`에 저장됩니다(브라우저 / 프로필 단위): 글자 크기, 머리글 / 공백
정리 / 문장 줄바꿈 토글, 인코딩, 구분자, BOM, 자동 다음 행 이동, 라벨 프리셋, 테마, 패널 너비, 접힘
상태, 영역 분할 비율. 이 설정들은 특정 파일에 묶이지 **않습니다**.

**개인정보.** 이 앱은 네트워크 요청을 전혀 하지 않습니다. CSV는 로컬에서 읽히며 기기를 떠나지 않습니다.

## 12. 키보드 단축키

| 상황 | 키 | 동작 |
|---|---|---|
| 이동 | `←↑↓→` | 셀 한 칸 이동 |
| 이동 | `Home` / `End` | 첫 / 마지막 열 |
| 이동 | `Ctrl/⌘ + Home` / `End` | 첫 / 마지막 행 |
| 이동 | `PageUp` / `PageDown` | 10행 이동 |
| 이동 | `1`–`9` | 주석 열에 프리셋 라벨 입력 |
| 이동 | `Enter` / `F2` | 현재 행에 코멘트 편집 |
| 편집 | `Ctrl/⌘ + Enter` | 코멘트 확정, 다음 행으로 이동 |
| 편집 | `Esc` | 코멘트 확정, 편집 종료 |
| 편집 | `Tab` / `Shift + Tab` | 확정, 오른쪽 / 왼쪽으로 이동 |
| 전역 | `Ctrl/⌘ + S` | 저장 |
| 전역 | `Ctrl/⌘ + C` | 원본 셀 복사 |

## 13. 팁과 한계

- **대용량 파일**은 전체를 메모리에 올려 한 번에 파싱하지만, 한 셀만 렌더링하므로 이동은 빠릅니다.
  스트리밍 / 가상화는 없으므로 아주 큰 파일은 가용 메모리(RAM)에 의해 제한됩니다.
- **문장 줄바꿈**은 의도적으로 보수적입니다. 연도(`1990.`), 도메인(`example.com.`), 약칭은 끊지
  않습니다. 필요하면 `index.html`의 약어 목록을 확장할 수 있습니다.
- **원본 파일에 바로 저장**은 Chromium + 앱 내 선택창으로 연 파일이 필요합니다. 드래그 앤 드롭이나 다른
  브라우저는 다운로드로 대체됩니다.
- **저장 시 인코딩**은 항상 UTF-8입니다(§9 참고).
- 주석 대상으로 **기존 열을 편집**하면 그 값이 덮어써집니다. 원본을 보존하려면 **Add new column**을
  사용하세요.

## 14. 자주 묻는 질문

**한글이 깨져 보여요.** **인코딩**을 `EUC-KR / CP949`로 설정하거나, UTF-8로 다시 내보내세요.

**방향키가 셀을 옮기지 않고 글자 커서만 움직여요.** `EDIT` 모드(코멘트 입력 중)입니다. `Esc`를 눌러 이동
모드로 돌아가세요.

**`Enter`로 코멘트가 저장되지 않아요.** 여러 줄 셀에서 `Enter`는 줄바꿈을 넣습니다. `Ctrl/⌘ + Enter`나
`Esc`로 확정하세요.

**프리셋 / 테마가 다른 컴퓨터로 이어지지 않아요.** 설정은 브라우저 단위로 저장되며 동기화되지 않습니다.

**저장이 덮어쓰지 않고 사본을 다운로드했어요.** 브라우저가 원본 저장을 지원하지 않거나, 파일을
**Open CSV…**로 열지 않은 경우입니다. Chrome / Edge에서 앱 내 선택창을 사용하세요.
